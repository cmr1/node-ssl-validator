#!/usr/bin/env node

'use strict';

const config = require('../config');
const SslValidator = require('../');

const sslValidator = new SslValidator(config);

sslValidator.run(err => {
  if (err) sslValidator.fail(err);
});